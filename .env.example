# Copy to `.env` and fill in from your Supabase project settings (API section).
# Both values are safe for the frontend; security is enforced by Row-Level Security.
# NEVER put the service_role key here – it must stay server-side only.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
